{
  "name": "arbiDex",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "front-start": "npm run front-start --prefix front",
    "front-build": "npm run build --prefix front",

    "back-start": "npm run back-start --prefix back",
    "back-start:dev": "npm run start:dev --prefix back",
    "back-build": "npm run build --prefix back",

    "start:docker": "docker-compose up --build -d",
    "stop & clear:docker": "docker compose down -v",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
